# Effective Brainlink canon — documentation reconciliation snapshot 2026-08-08

## Current effective facts

- Repository identity: `MRTNLGDR/BRAINLINK`.
- Upstream base: AFFiNE `v0.27.0`, commit `c61cc6a86f5f8364732296f0bb8393b37e0f70b3`.
- Promoted Brainlink runtime: `v2.1`.
- Candidate runtime: `v2.2`, **not promoted**.
- Persisted Brainlink state schema: `2`.
- Stable runtime includes scoped Universalis laws/read receipts, evidence-gated task completion, approval-gated connector write capability, explicit bug verification, v1→v2 state migration/import validation, and SHA-256 chained audit integrity.
- Brainlink remains the knowledge/governance plane. Oráculo, COSMETA, Ultrabase and AIIA Gateway remain adjacent/external boundaries unless separately integrated with real inspected runtimes.
- V5 is context-only and cannot change stable runtime by documentation alone.

## Effective non-breakage rules

1. Historical sources are never overwritten; they are superseded by explicit records only.
2. No target spec becomes `IMPLEMENTATION_VERIFIED` without execution evidence.
3. No external integration becomes healthy/connected without a real transport/runtime.
4. AFFiNE surfaces are reused/extended before core rewrites.
5. Current stable runtime remains recoverable during candidate work.
6. A candidate cannot become stable because a newer document exists.
7. Product boundaries are preserved: Brainlink governs; it does not absorb COSMETA/Oráculo/Ultrabase runtime responsibilities.
8. Current status claims use latest verified evidence; old status claims remain historical.
9. Version axes are explicit and cannot be conflated.
10. Missing historical artifacts are recorded, never reconstructed deceptively.

## Source adoption policy

### V1 documentation package

Classification: `HISTORICAL_SNAPSHOT + NORMATIVE_SOURCE_COMPONENTS + BRAINLINK_TARGET_SPEC`.

- Preserve exact archive bytes and per-file hashes.
- Preserve all readable text verbatim.
- Normative components may inform canon when not superseded/conflicted.
- Historical implementation-state claims do not downgrade current verified runtime.

### V4 embedded in V5

Classification: `HISTORICAL_CUMULATIVE_SNAPSHOT / BRAINLINK_TARGET_SPEC`.

- Valuable for lineage, 44-screen catalog and missing-screen research.
- Its statement that runtime/screens were not implemented is historical, not current.

### V5 integrated document

Classification: `CROSS_PRODUCT_TARGET_SPEC / CONTEXT_COMPLEMENT_ONLY`.

Compatible Brainlink ideas may be adopted only through candidate gates. Oráculo/COSMETA/voice/model/vendor/filesystem-runtime sections remain adjacent-system context unless a separate project owns them.

## Current unresolved external boundaries

- Ultrabase real repo/manual/schema/migrations/RLS/storage/realtime.
- Production MCP transports and inspected servers.
- Secret broker/OpenBao integration.
- Provider credentials and remote services.
- User-machine C:/D: inventory/migration.
- Full AFFiNE dependency install/build/browser E2E in an environment with package registry access.

These stay visible as blockers; they are never converted into fake success.
