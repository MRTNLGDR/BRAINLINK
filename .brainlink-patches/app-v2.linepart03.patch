   const renderDocuments = (canvas = false) => <div className="bl-grid"><Card title={canvas ? 'BlockSuite Canvas integration' : 'AFFiNE document engine integration'} span="8"><p>{canvas ? 'Brainlink preserves the upstream BlockSuite canvas instead of reimplementing it. Use the AFFiNE workspace for real editing; Brainlink governance remains available under /brainlink.' : 'Brainlink preserves AFFiNE documents, CRDT and local-first workspace behavior. This runtime adds governance without replacing the upstream document engine.'}</p><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" data-primary="true" onClick={() => navigate('/')}>Open workspace</button><button className="bl-btn" onClick={() => navigateTo('audit')}>View governance ledger</button></div></Card><Card title="Integration contract" span="4"><p>Upstream surface reuse is intentional: no fake document editor or disconnected canvas was added.</p><div className="bl-meta"><Badge tone="good">UPSTREAM REUSE</Badge><Badge>LOCAL-FIRST</Badge></div></Card></div>;
 
@@ -429,7 +529,7 @@
     if (rawSlug === 'superadmin/roadmap' || rawSlug === 'superadmin/tasks') return rawSlug.endsWith('roadmap') ? renderRoadmap() : renderTasks();
     if (rawSlug === 'superadmin/workers') return renderWorkers();
     if (rawSlug === 'superadmin/rules') return renderUniversalis();
-    if (rawSlug === 'superadmin/approvals') return renderDashboard();
+    if (rawSlug === 'superadmin/approvals') return renderApprovals();
     if (rawSlug === 'superadmin/alerts') return renderBugs();
     if (rawSlug === 'superadmin/evidence') return <div className="bl-grid"><Card title="Evidence" span="12">{renderEvidence()}</Card><Card title="Audit" span="12">{renderAudit()}</Card></div>;
     if (rawSlug === 'superadmin/connections') return renderConnections();
@@ -439,7 +539,7 @@
     if (rawSlug === 'superadmin/modules') return <div className="bl-grid">{BRAINLINK_SCREENS.filter(item => item.area !== 'MOBILE').map(item => <Card key={item.id} title={item.label} span="4"><p>{item.description}</p><div className="bl-meta"><Badge tone="good">ROUTED</Badge><Badge>{item.id}</Badge></div></Card>)}</div>;
     if (rawSlug === 'superadmin/documents-index') return renderDocuments(false);
     if (rawSlug === 'superadmin/secrets') return <div className="bl-grid"><Card title="Secret broker boundary" span="12"><p>Brainlink stores connector metadata and secret references only. No field in this runtime accepts or renders secret values.</p><div className="bl-meta"><Badge tone="good">NO SECRET VALUES IN STATE</Badge></div></Card></div>;
-    if (rawSlug === 'superadmin/data-migrations') return <div className="bl-grid"><Card title="Data plane" span="6"><div className="bl-kpi">v1</div><p>Local Brainlink state schema is versioned and exportable. Ultrabase remains an adapter boundary; no fake migration is claimed.</p></Card><Card title="Migration safety" span="6"><p>Import validates schemaVersion and required collections before replacing local state. Every successful import records an audit event.</p><div className="bl-meta"><Badge tone="good">VALIDATED IMPORT</Badge></div></Card></div>;
+    if (rawSlug === 'superadmin/data-migrations') return <div className="bl-grid"><Card title="Data plane" span="6"><div className="bl-kpi">v2</div><p>Local Brainlink state schema is versioned and exportable. Ultrabase remains an adapter boundary; no fake migration is claimed.</p></Card><Card title="Migration safety" span="6"><p>Import validates schemaVersion and required collections before replacing local state. Every successful import records an audit event.</p><div className="bl-meta"><Badge tone="good">VALIDATED IMPORT</Badge></div></Card></div>;
     if (rawSlug === 'superadmin/licenses') return <div className="bl-grid"><Card title="Fork provenance" span="12"><p>Brainlink is built from AFFiNE Community Edition and preserves upstream LICENSE/notices. The repository keeps Brainlink-specific changes isolated and documents upstream reuse.</p><div className="bl-meta"><Badge tone="good">MIT UPSTREAM PRESERVED</Badge><Badge>AFFiNE 0.27.0 SNAPSHOT</Badge></div></Card></div>;
     return renderDashboard();
   };
@@ -450,22 +550,24 @@
     if (kind === 'projects') {
       const project = state.projects.find(item => item.id === id); if (!project) return <div className="bl-empty">Project not found.</div>;
       const tasks = state.tasks.filter(task => task.projectId === project.id);
-      return <div className="bl-grid"><Card title={project.name} span="8"><p>{project.description}</p><div className="bl-meta"><Badge>health {project.health}</Badge><Badge>{project.progress}%</Badge></div><div className="bl-progress"><span style={{ width:`${project.progress}%` }} /></div></Card><Card title="Project metrics" span="4"><div className="bl-kpi">{tasks.length}<small>tasks</small></div><p>{tasks.filter(task => task.status === 'DONE').length} done · {tasks.filter(task => task.status === 'BLOCKED').length} blocked</p></Card><Card title="Tasks" span="12"><div className="bl-list">{tasks.map(task => <div className="bl-row" key={task.id}><div><strong>{task.title}</strong><small>{task.owner}</small></div><Badge tone={toneForStatus(task.status)}>{task.status}</Badge></div>)}</div></Card></div>;
+      const progress = projectProgress(state, project.id);
+      return <div className="bl-grid"><Card title={project.name} span="8"><p>{project.description}</p><div className="bl-meta"><Badge>health {project.health}</Badge><Badge>{progress}%</Badge></div><div className="bl-progress"><span style={{ width:`${progress}%` }} /></div></Card><Card title="Project metrics" span="4"><div className="bl-kpi">{tasks.length}<small>tasks</small></div><p>{tasks.filter(task => task.status === 'DONE').length} done · {tasks.filter(task => task.status === 'BLOCKED').length} blocked</p></Card><Card title="Tasks" span="12"><div className="bl-list">{tasks.map(task => <div className="bl-row" key={task.id}><div><strong>{task.title}</strong><small>{task.owner}</small></div><Badge tone={toneForStatus(task.status)}>{task.status}</Badge></div>)}</div></Card></div>;
     }
     if (kind === 'tasks') {
       const task = state.tasks.find(item => item.id === id); if (!task) return <div className="bl-empty">Task not found.</div>;
       const evidence = state.evidence.filter(item => item.taskId === task.id);
-      return <div className="bl-grid"><Card title={task.title} span="8"><p>Owner: {task.owner}</p><div className="bl-meta"><Badge tone={toneForStatus(task.status)}>{task.status}</Badge><Badge>{task.evidenceRequired ? 'EVIDENCE REQUIRED' : 'NO EVIDENCE GATE'}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}>{(['READY','IN_PROGRESS','BLOCKED','DONE'] as BrainlinkTaskStatus[]).map(status => <button className="bl-btn" key={status} onClick={() => changeTaskStatus(task.id,status)}>{status}</button>)}</div></Card><Card title="Evidence" span="4"><div className="bl-kpi">{evidence.length}</div><p>records attached</p><button className="bl-btn" style={{ marginTop:12 }} onClick={() => navigateTo('audit')}>Open ledger</button></Card><Card title="Attached evidence" span="12"><div className="bl-list">{evidence.map(item => <div className="bl-row" key={item.id}><div><strong>{item.title}</strong><small>{item.detail}</small></div><Badge>{item.type}</Badge></div>)}{evidence.length === 0 ? <div className="bl-empty">No evidence attached; DONE will be blocked while evidence gate is enabled.</div> : null}</div></Card></div>;
+      const blockers = taskCompletionBlockers(state, task, state.settings.actorName);
+      return <div className="bl-grid"><Card title={task.title} span="8"><p>Owner: {task.owner}</p><div className="bl-meta"><Badge tone={toneForStatus(task.status)}>{task.status}</Badge><Badge>{task.evidenceRequired ? 'EVIDENCE REQUIRED' : 'NO EVIDENCE GATE'}</Badge><Badge tone={blockers.length ? 'warn' : 'good'}>{blockers.length ? `DONE BLOCKED · ${blockers.join(' + ')}` : 'DONE GATE READY'}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}>{(['READY','IN_PROGRESS','BLOCKED','DONE'] as BrainlinkTaskStatus[]).map(status => <button className="bl-btn" key={status} onClick={() => changeTaskStatus(task.id,status)}>{status}</button>)}</div></Card><Card title="Evidence" span="4"><div className="bl-kpi">{evidence.length}</div><p>records attached</p><button className="bl-btn" style={{ marginTop:12 }} onClick={() => navigateTo('audit')}>Open ledger</button></Card><Card title="Attached evidence" span="12"><div className="bl-list">{evidence.map(item => <div className="bl-row" key={item.id}><div><strong>{item.title}</strong><small>{item.detail}</small></div><Badge>{item.type}</Badge></div>)}{evidence.length === 0 ? <div className="bl-empty">No evidence attached; DONE will be blocked while evidence gate is enabled.</div> : null}</div></Card></div>;
     }
     const worker = state.workers.find(item => item.id === id); if (!worker) return <div className="bl-empty">Worker not found.</div>;
-    return <div className="bl-grid"><Card title={worker.name} span="8"><p>{worker.role}</p><div className="bl-meta"><Badge tone={toneForStatus(worker.status)}>{worker.status}</Badge><Badge tone={workerAuthorized(worker) ? 'good' : 'warn'}>{workerAuthorized(worker) ? 'AUTHORIZED' : 'READ GATE REQUIRED'}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" onClick={() => authorizeWorker(worker)}>Acknowledge all laws</button><button className="bl-btn" onClick={() => setWorkerStatus(worker,'RUNNING')}>Start</button><button className="bl-btn" onClick={() => setWorkerStatus(worker,'PAUSED')}>Pause</button></div></Card><Card title="Lifecycle" span="4"><p>Last heartbeat</p><div className="bl-kpi" style={{ fontSize:16 }}>{formatDate(worker.lastHeartbeat)}</div></Card></div>;
+    return <div className="bl-grid"><Card title={worker.name} span="8"><p>{worker.role}</p><div className="bl-meta"><Badge tone={toneForStatus(worker.status)}>{worker.status}</Badge><Badge tone={workerAuthorized(worker) ? 'good' : 'warn'}>{workerAuthorized(worker) ? 'AUTHORIZED' : `READ ${requiredLawsForWorker(worker).length} LAW(S)`}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" onClick={() => authorizeWorker(worker)}>Acknowledge effective laws</button><button className="bl-btn" onClick={() => setWorkerStatus(worker,'RUNNING')}>Start</button><button className="bl-btn" onClick={() => setWorkerStatus(worker,'PAUSED')}>Pause</button><button className="bl-btn" onClick={() => setWorkerStatus(worker,'BLOCKED')}>Block / error</button></div></Card><Card title="Lifecycle" span="4"><p>Last heartbeat</p><div className="bl-kpi" style={{ fontSize:16 }}>{formatDate(worker.lastHeartbeat)}</div></Card></div>;
   };
 
   const renderMobile = () => {
     if (rawSlug === 'projects') return renderProjects();
     if (rawSlug === 'tasks') return renderTasks();
     if (rawSlug === 'workers') return renderWorkers();
-    if (rawSlug === 'approvals') return renderDashboard();
+    if (rawSlug === 'approvals') return renderApprovals();
     if (rawSlug === 'evidence') return renderEvidence();
     if (rawSlug === 'notifications') return <div className="bl-grid"><Card title="Notifications" span="12"><div className="bl-list">{state.notifications.map(item => <button className="bl-row" style={{ width:'100%',color:'inherit',cursor:'pointer',textAlign:'left' }} key={item.id} onClick={() => commit('NOTIFICATION_READ', item.id, draft => { const notification = draft.notifications.find(entry => entry.id === item.id); if (notification) notification.read = true; })}><div><strong>{item.title}</strong><small>{item.detail}</small></div><Badge tone={item.read ? 'good' : 'warn'}>{item.read ? 'READ' : 'NEW'}</Badge></button>)}</div></Card></div>;
     if (rawSlug === 'chat') return <div className="bl-grid"><Card title="Realtime AI Chat adapter surface" span="12"><p>The UI boundary is routed and ready, but Brainlink does not invent an LLM transport. Connectors belong in the governed MCP/API adapter layer and must never bypass Universalis or secret-reference policy.</p><div className="bl-meta"><Badge>ADAPTER BOUNDARY</Badge><Badge tone="good">NO FAKE MODEL CALLS</Badge></div></Card></div>;
