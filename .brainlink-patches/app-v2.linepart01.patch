+        scope,
+        targetId,
+        trigger: String(data.get('trigger') || 'ALWAYS') as 'ALWAYS' | 'SESSION_START' | 'TASK_START' | 'TASK_END' | 'ON_ERROR',
+        version: 1,
+        body: String(data.get('body') || ''),
+        mandatory: data.get('mandatory') === 'on',
+        enabled: true,
+        createdAt: new Date().toISOString(),
+      }));
+      event.currentTarget.reset();
     };
-    return <div className="bl-split"><section><div className="bl-list">{state.laws.map(law => { const acknowledged = state.receipts.some(receipt => receipt.actor === state.settings.actorName && receipt.lawId === law.id && receipt.lawVersion === law.version); return <div className="bl-row" key={law.id}><div className="bl-row-main"><strong>{law.id} · {law.title}</strong><small>{law.body}</small><div className="bl-meta"><Badge>{law.scope}</Badge><Badge>v{law.version}</Badge>{law.mandatory ? <Badge tone="warn">MANDATORY</Badge> : null}{!law.enabled ? <Badge tone="bad">DISABLED</Badge> : null}</div></div><div className="bl-actions"><button className="bl-btn" onClick={() => acknowledgeLaw(law.id)}>{acknowledged ? 'Re-acknowledge' : 'Acknowledge'}</button><button className="bl-btn" onClick={() => commit('LAW_VERSION_BUMPED', law.id, draft => { const item = draft.laws.find(entry => entry.id === law.id); if (item) item.version += 1; })}>New version</button><button className="bl-btn" onClick={() => commit('LAW_TOGGLED', law.id, draft => { const item = draft.laws.find(entry => entry.id === law.id); if (item) item.enabled = !item.enabled; })}>{law.enabled ? 'Disable' : 'Enable'}</button></div></div>; })}</div></section><Card title="New law" span="12"><form className="bl-form" onSubmit={addLaw}><div className="bl-field" data-span="12"><label>Title</label><input name="title" required /></div><div className="bl-field" data-span="12"><label>Rule</label><textarea name="body" required /></div><div className="bl-field" data-span="12"><label><input type="checkbox" name="mandatory" style={{ width: 'auto', marginRight: 8 }} /> Mandatory read gate</label></div><div className="bl-field" data-span="12"><button className="bl-btn" data-primary="true">Publish v1</button></div></form></Card></div>;
+    return <div className="bl-split"><section><div className="bl-list">{state.laws.map(law => {
+      const acknowledged = state.receipts.some(receipt => receipt.actor === state.settings.actorName && receipt.lawId === law.id && receipt.lawVersion === law.version);
+      return <div className="bl-row" key={law.id}><div className="bl-row-main"><strong>{law.id} · {law.title}</strong><small>{law.body}</small><div className="bl-meta"><Badge>{law.scope}{law.targetId ? `:${law.targetId}` : ''}</Badge><Badge>{law.trigger}</Badge><Badge>v{law.version}</Badge>{law.mandatory ? <Badge tone="warn">MANDATORY</Badge> : null}{!law.enabled ? <Badge tone="bad">DISABLED</Badge> : null}</div></div><div className="bl-actions"><button className="bl-btn" onClick={() => acknowledgeLaw(law.id)}>{acknowledged ? 'Re-acknowledge' : 'Acknowledge'}</button><button className="bl-btn" onClick={() => commit('LAW_VERSION_BUMPED', law.id, draft => { const item = draft.laws.find(entry => entry.id === law.id); if (item) item.version += 1; })}>New version</button><button className="bl-btn" onClick={() => commit('LAW_TOGGLED', law.id, draft => { const item = draft.laws.find(entry => entry.id === law.id); if (item) item.enabled = !item.enabled; })}>{law.enabled ? 'Disable' : 'Enable'}</button></div></div>;
+    })}</div></section><Card title="New law" span="12"><form className="bl-form" onSubmit={addLaw}><div className="bl-field" data-span="12"><label>Title</label><input name="title" required /></div><div className="bl-field"><label>Scope</label><select name="scope"><option>GLOBAL</option><option>ORGANIZATION</option><option>PROJECT</option><option>WORKER</option></select></div><div className="bl-field"><label>Target ID</label><input name="targetId" placeholder="Required for scoped laws" /></div><div className="bl-field"><label>Trigger</label><select name="trigger"><option>ALWAYS</option><option>SESSION_START</option><option>TASK_START</option><option>TASK_END</option><option>ON_ERROR</option></select></div><div className="bl-field" data-span="12"><label>Rule</label><textarea name="body" required /></div><div className="bl-field" data-span="12"><label><input type="checkbox" name="mandatory" style={{ width: 'auto', marginRight: 8 }} /> Mandatory read gate</label></div><div className="bl-field" data-span="12"><button className="bl-btn" data-primary="true">Publish v1</button></div></form></Card></div>;
   };
 
   const renderTasks = () => {
@@ -390,7 +436,7 @@
       event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get('name') || '').trim(); if (!name) return;
       commit('WORKER_CREATED', name, draft => draft.workers.unshift({ id: createBrainlinkId('WORKER'), name, role: String(data.get('role') || 'Worker'), status: 'IDLE', currentTaskId: String(data.get('taskId') || '') || undefined, createdAt: new Date().toISOString() })); event.currentTarget.reset();
     };
-    return <div className="bl-grid"><Card title="Register worker" span="12"><form className="bl-form" onSubmit={addWorker}><div className="bl-field"><label>Name</label><input name="name" required /></div><div className="bl-field"><label>Role</label><input name="role" /></div><div className="bl-field"><label>Task</label><select name="taskId"><option value="">None</option>{state.tasks.map(task => <option value={task.id} key={task.id}>{task.title}</option>)}</select></div><div className="bl-field"><label>&nbsp;</label><button className="bl-btn" data-primary="true">Register</button></div></form></Card>{state.workers.map(worker => <Card key={worker.id} title={worker.name} span="6"><p>{worker.role}</p><div className="bl-meta"><Badge tone={toneForStatus(worker.status)}>{worker.status}</Badge><Badge tone={workerAuthorized(worker) ? 'good' : 'warn'}>{workerAuthorized(worker) ? 'READ GATE OK' : 'READ GATE REQUIRED'}</Badge></div><p style={{ marginTop: 10 }}>Heartbeat: {formatDate(worker.lastHeartbeat)}</p><div className="bl-actions" style={{ marginTop: 12 }}>{!workerAuthorized(worker) ? <button className="bl-btn" onClick={() => authorizeWorker(worker)}>Acknowledge laws</button> : null}<button className="bl-btn" onClick={() => setWorkerStatus(worker, 'RUNNING')}>Start</button><button className="bl-btn" onClick={() => setWorkerStatus(worker, 'PAUSED')}>Pause</button><button className="bl-btn" onClick={() => setWorkerStatus(worker, 'IDLE')}>Stop</button><button className="bl-btn" onClick={() => navigate(`${base}/workers/${worker.id}`)}>Detail</button></div></Card>)}</div>;
+    return <div className="bl-grid"><Card title="Register worker" span="12"><form className="bl-form" onSubmit={addWorker}><div className="bl-field"><label>Name</label><input name="name" required /></div><div className="bl-field"><label>Role</label><input name="role" /></div><div className="bl-field"><label>Task</label><select name="taskId"><option value="">None</option>{state.tasks.map(task => <option value={task.id} key={task.id}>{task.title}</option>)}</select></div><div className="bl-field"><label>&nbsp;</label><button className="bl-btn" data-primary="true">Register</button></div></form></Card>{state.workers.map(worker => <Card key={worker.id} title={worker.name} span="6"><p>{worker.role}</p><div className="bl-meta"><Badge tone={toneForStatus(worker.status)}>{worker.status}</Badge><Badge tone={workerAuthorized(worker) ? 'good' : 'warn'}>{workerAuthorized(worker) ? 'READ GATE OK' : `READ ${requiredLawsForWorker(worker).length} LAW(S)`}</Badge></div><p style={{ marginTop: 10 }}>Heartbeat: {formatDate(worker.lastHeartbeat)}</p><div className="bl-actions" style={{ marginTop: 12 }}>{!workerAuthorized(worker) ? <button className="bl-btn" onClick={() => authorizeWorker(worker)}>Acknowledge laws</button> : null}<button className="bl-btn" onClick={() => setWorkerStatus(worker, 'RUNNING')}>Start</button><button className="bl-btn" onClick={() => setWorkerStatus(worker, 'PAUSED')}>Pause</button><button className="bl-btn" onClick={() => setWorkerStatus(worker, 'BLOCKED')}>Block / error</button><button className="bl-btn" onClick={() => setWorkerStatus(worker, 'IDLE')}>Stop</button><button className="bl-btn" onClick={() => navigate(`${base}/workers/${worker.id}`)}>Detail</button></div></Card>)}</div>;
   };
 
   const renderEvidence = () => {
@@ -401,11 +447,31 @@
     return <div className="bl-grid"><Card title="Attach evidence" span="12"><form className="bl-form" onSubmit={addEvidence}><div className="bl-field"><label>Title</label><input name="title" required /></div><div className="bl-field"><label>Task</label><select name="taskId"><option value="">General</option>{state.tasks.map(task => <option key={task.id} value={task.id}>{task.title}</option>)}</select></div><div className="bl-field" data-span="6"><label>Detail / hash / path / test result</label><input name="detail" /></div><div className="bl-field"><label>&nbsp;</label><button className="bl-btn" data-primary="true">Attach</button></div></form></Card><Card title="Evidence ledger" span="12"><table className="bl-table"><thead><tr><th>Time</th><th>Type</th><th>Title</th><th>Actor</th><th>Task</th></tr></thead><tbody>{state.evidence.map(item => <tr key={item.id}><td>{formatDate(item.createdAt)}</td><td><Badge>{item.type}</Badge></td><td>{item.title}<small style={{ display:'block',color:'#727b86',marginTop:3 }}>{item.detail}</small></td><td>{item.actor}</td><td>{item.taskId || '—'}</td></tr>)}</tbody></table></Card></div>;
   };
 
-  const renderAudit = () => <Card title="Immutable-in-app audit ledger" span="12"><table className="bl-table"><thead><tr><th>Time</th><th>Action</th><th>Detail</th><th>Actor</th></tr></thead><tbody>{state.audit.map(item => <tr key={item.id}><td>{formatDate(item.createdAt)}</td><td>{item.action}</td><td>{item.detail}</td><td>{item.actor}</td></tr>)}</tbody></table></Card>;
+  const renderAudit = () => <Card title="Append-only application audit ledger" span="12"><table className="bl-table"><thead><tr><th>Time</th><th>Action</th><th>Detail</th><th>Actor</th></tr></thead><tbody>{state.audit.map(item => <tr key={item.id}><td>{formatDate(item.createdAt)}</td><td>{item.action}</td><td>{item.detail}</td><td>{item.actor}</td></tr>)}</tbody></table></Card>;
 
   const renderBugs = () => {
-    const addBug = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const bugTitle = String(data.get('title') || '').trim(); if (!bugTitle) return; commit('BUG_RECORDED', bugTitle, draft => draft.bugs.unshift({ id: createBrainlinkId('BUG'), title: bugTitle, severity: 'MEDIUM', status: 'OPEN', cause: '', solution: '', createdAt: new Date().toISOString() })); event.currentTarget.reset(); };
-    return <div className="bl-grid"><Card title="Record bug" span="12"><form className="bl-form" onSubmit={addBug}><div className="bl-field" data-span="8"><label>Bug</label><input name="title" required /></div><div className="bl-field"><label>&nbsp;</label><button className="bl-btn" data-primary="true">Record</button></div></form></Card>{state.bugs.map(bug => <Card key={bug.id} title={bug.title} span="6"><div className="bl-meta"><Badge tone={toneForStatus(bug.severity)}>{bug.severity}</Badge><Badge tone={toneForStatus(bug.status)}>{bug.status}</Badge></div><div className="bl-form" style={{ marginTop: 12 }}><div className="bl-field" data-span="12"><label>Cause</label><textarea value={bug.cause} onChange={event => commit('BUG_CAUSE_UPDATED', bug.id, draft => { const item = draft.bugs.find(entry => entry.id === bug.id); if (item) item.cause = event.target.value; })} /></div><div className="bl-field" data-span="12"><label>Verified solution</label><textarea value={bug.solution} onChange={event => commit('BUG_SOLUTION_UPDATED', bug.id, draft => { const item = draft.bugs.find(entry => entry.id === bug.id); if (item) { item.solution = event.target.value; if (event.target.value.trim()) item.status = 'SOLVED'; } })} /></div></div></Card>)}{state.bugs.length === 0 ? <Card span="12"><div className="bl-empty">No bugs recorded.</div></Card> : null}</div>;
+    const addBug = (event: FormEvent<HTMLFormElement>) => {
+      event.preventDefault();
+      const data = new FormData(event.currentTarget);
+      const bugTitle = String(data.get('title') || '').trim();
+      if (!bugTitle) return;
+      commit('BUG_RECORDED', bugTitle, draft => draft.bugs.unshift({ id: createBrainlinkId('BUG'), title: bugTitle, severity: 'MEDIUM', status: 'OPEN', cause: '', solution: '', createdAt: new Date().toISOString() }));
+      event.currentTarget.reset();
+    };
+    const solveBug = (bugId: string) => {
+      const bug = state.bugs.find(item => item.id === bugId);
+      if (!bug?.solution.trim()) {
+        setToast('A verified solution description is required before SOLVED.');
+        return;
+      }
+      commit('BUG_VERIFIED_SOLVED', bug.title, draft => {
+        const item = draft.bugs.find(entry => entry.id === bugId);
+        if (!item) return;
+        item.status = 'SOLVED';
+        draft.evidence.unshift({ id: createBrainlinkId('EVID'), type: 'TEST', title: `Bug solved: ${item.title}`, detail: item.solution, actor: draft.settings.actorName, createdAt: new Date().toISOString() });
+      });
+    };
