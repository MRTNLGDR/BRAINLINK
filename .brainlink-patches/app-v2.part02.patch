n;
+      }
+      commit('BUG_VERIFIED_SOLVED', bug.title, draft => {
+        const item = draft.bugs.find(entry => entry.id === bugId);
+        if (!item) return;
+        item.status = 'SOLVED';
+        draft.evidence.unshift({ id: createBrainlinkId('EVID'), type: 'TEST', title: `Bug solved: ${item.title}`, detail: item.solution, actor: draft.settings.actorName, createdAt: new Date().toISOString() });
+      });
+    };
+    return <div className="bl-grid"><Card title="Record bug" span="12"><form className="bl-form" onSubmit={addBug}><div className="bl-field" data-span="8"><label>Bug</label><input name="title" required /></div><div className="bl-field"><label>&nbsp;</label><button className="bl-btn" data-primary="true">Record</button></div></form></Card>{state.bugs.map(bug => <Card key={bug.id} title={bug.title} span="6"><div className="bl-meta"><Badge tone={toneForStatus(bug.severity)}>{bug.severity}</Badge><Badge tone={toneForStatus(bug.status)}>{bug.status}</Badge></div><div className="bl-form" style={{ marginTop: 12 }}><div className="bl-field" data-span="12"><label>Cause</label><textarea value={bug.cause} onChange={event => commit('BUG_CAUSE_UPDATED', bug.id, draft => { const item = draft.bugs.find(entry => entry.id === bug.id); if (item) { item.cause = event.target.value; if (item.status === 'OPEN') item.status = 'INVESTIGATING'; } })} /></div><div className="bl-field" data-span="12"><label>Candidate solution</label><textarea value={bug.solution} onChange={event => commit('BUG_SOLUTION_UPDATED', bug.id, draft => { const item = draft.bugs.find(entry => entry.id === bug.id); if (item) { item.solution = event.target.value; if (item.status !== 'SOLVED') item.status = 'INVESTIGATING'; } })} /></div><div className="bl-field" data-span="12"><button className="bl-btn" data-primary="true" disabled={!bug.solution.trim() || bug.status === 'SOLVED'} onClick={() => solveBug(bug.id)}>{bug.status === 'SOLVED' ? 'Verified solved' : 'Verify and mark solved'}</button></div></div></Card>)}{state.bugs.length === 0 ? <Card span="12"><div className="bl-empty">No bugs recorded.</div></Card> : null}</div>;
   };
 
   const renderArchetypes = () => {
@@ -414,13 +480,47 @@
   };
 
   const renderConnections = () => {
-    const addConnection = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get('name') || '').trim(); if (!name) return; commit('CONNECTION_REGISTERED', name, draft => draft.connections.unshift({ id: createBrainlinkId('CONN'), name, kind: String(data.get('kind') || 'MCP') as 'MCP' | 'GIT' | 'FILESYSTEM' | 'API', endpoint: String(data.get('endpoint') || ''), mode: 'READ_ONLY', status: 'DISCONNECTED', createdAt: new Date().toISOString() })); event.currentTarget.reset(); };
-    return <div className="bl-grid"><Card title="Register connector (metadata only — no secret values)" span="12"><form className="bl-form" onSubmit={addConnection}><div className="bl-field"><label>Name</label><input name="name" required /></div><div className="bl-field"><label>Kind</label><select name="kind"><option>MCP</option><option>GIT</option><option>FILESYSTEM</option><option>API</option></select></div><div className="bl-field" data-span="6"><label>Endpoint / root / URI</label><input name="endpoint" /></div><div className="bl-field"><button className="bl-btn" data-primary="true">Register</button></div></form></Card>{state.connections.map(connection => <Card key={connection.id} title={connection.name} span="6"><p>{connection.kind} · {connection.endpoint || 'No endpoint'}</p><div className="bl-meta"><Badge>{connection.mode}</Badge><Badge tone={toneForStatus(connection.status)}>{connection.status}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" onClick={() => commit('CONNECTION_STATUS_CHANGED', connection.id, draft => { const item = draft.connections.find(entry => entry.id === connection.id); if (item) item.status = item.status === 'CONNECTED' ? 'DISCONNECTED' : 'CONNECTED'; })}>{connection.status === 'CONNECTED' ? 'Disconnect' : 'Mark connected'}</button><button className="bl-btn" onClick={() => commit('CONNECTION_MODE_CHANGED', connection.id, draft => { const item = draft.connections.find(entry => entry.id === connection.id); if (item) item.mode = item.mode === 'READ_ONLY' ? 'READ_WRITE' : 'READ_ONLY'; })}>Toggle capability</button></div></Card>)}</div>;
+    const addConnection = (event: FormEvent<HTMLFormElement>) => {
+      event.preventDefault();
+      const data = new FormData(event.currentTarget);
+      const name = String(data.get('name') || '').trim();
+      if (!name) return;
+      commit('CONNECTION_REGISTERED', name, draft => draft.connections.unshift({ id: createBrainlinkId('CONN'), name, kind: String(data.get('kind') || 'MCP') as 'MCP' | 'GIT' | 'FILESYSTEM' | 'API', endpoint: String(data.get('endpoint') || ''), mode: 'READ_ONLY', status: 'REGISTERED', createdAt: new Date().toISOString() }));
+      event.currentTarget.reset();
+    };
+    const requestWriteAccess = (connectionId: string) => {
+      const connection = state.connections.find(item => item.id === connectionId);
+      if (!connection) return;
+      const existing = state.approvals.find(item => item.connectionId === connection.id && item.requestedMode === 'READ_WRITE' && item.status === 'PENDING');
+      if (existing) {
+        setToast('READ_WRITE approval is already pending.');
+        return;
+      }
+      commit('WRITE_ACCESS_REQUESTED', connection.name, draft => draft.approvals.unshift({ id: createBrainlinkId('APR'), title: `Enable READ_WRITE: ${connection.name}`, detail: `Human approval required before connector ${connection.id} may write.`, status: 'PENDING', connectionId: connection.id, requestedMode: 'READ_WRITE', createdAt: new Date().toISOString() }));
+      setToast('READ_WRITE approval requested.');
+    };
+    const setWriteMode = (connectionId: string, enabled: boolean) => {
+      const connection = state.connections.find(item => item.id === connectionId);
+      if (!connection) return;
+      if (enabled && !connectionHasApprovedWrite(state, connectionId)) {
+        setToast('READ_WRITE blocked: human approval is required first.');
+        return;
+      }
+      commit('CONNECTION_MODE_CHANGED', `${connection.name}: ${enabled ? 'READ_WRITE' : 'READ_ONLY'}`, draft => {
+        const item = draft.connections.find(entry => entry.id === connectionId);
+        if (item) item.mode = enabled ? 'READ_WRITE' : 'READ_ONLY';
+      });
+    };
+    return <div className="bl-grid"><Card title="Register connector metadata (no secret values)" span="12"><form className="bl-form" onSubmit={addConnection}><div className="bl-field"><label>Name</label><input name="name" required /></div><div className="bl-field"><label>Kind</label><select name="kind"><option>MCP</option><option>GIT</option><option>FILESYSTEM</option><option>API</option></select></div><div className="bl-field" data-span="6"><label>Endpoint / root / URI</label><input name="endpoint" /></div><div className="bl-field"><button className="bl-btn" data-primary="true">Register</button></div></form></Card>{state.connections.map(connection => {
+      const approved = connectionHasApprovedWrite(state, connection.id);
+      const pending = state.approvals.some(item => item.connectionId === connection.id && item.requestedMode === 'READ_WRITE' && item.status === 'PENDING');
+      return <Card key={connection.id} title={connection.name} span="6"><p>{connection.kind} · {connection.endpoint || 'No endpoint'}</p><div className="bl-meta"><Badge tone={connection.mode === 'READ_WRITE' ? 'warn' : undefined}>{connection.mode}</Badge><Badge>{connection.status}</Badge><Badge tone={approved ? 'good' : pending ? 'warn' : undefined}>{approved ? 'WRITE APPROVED' : pending ? 'APPROVAL PENDING' : 'NO WRITE APPROVAL'}</Badge></div><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" onClick={() => commit('CONNECTION_REGISTRATION_TOGGLED', connection.id, draft => { const item = draft.connections.find(entry => entry.id === connection.id); if (item) item.status = item.status === 'REGISTERED' ? 'DISABLED' : 'REGISTERED'; })}>{connection.status === 'REGISTERED' ? 'Disable metadata' : 'Enable metadata'}</button>{connection.mode === 'READ_WRITE' ? <button className="bl-btn" onClick={() => setWriteMode(connection.id, false)}>Revoke write</button> : approved ? <button className="bl-btn" data-primary="true" onClick={() => setWriteMode(connection.id, true)}>Enable READ_WRITE</button> : <button className="bl-btn" disabled={pending} onClick={() => requestWriteAccess(connection.id)}>{pending ? 'Approval pending' : 'Request READ_WRITE'}</button>}</div><p style={{ marginTop:10 }}>Registration does not claim a live transport connection. Actual connectivity must be verified by the future governed adapter.</p></Card>;
+    })}</div>;
   };
 
   const renderCalendar = () => <div className="bl-grid">{state.tasks.filter(task => task.dueAt).sort((a,b) => String(a.dueAt).localeCompare(String(b.dueAt))).map(task => <Card key={task.id} title={task.dueAt} span="4"><p>{task.title}</p><div className="bl-meta"><Badge tone={toneForStatus(task.status)}>{task.status}</Badge><Badge>{task.owner}</Badge></div></Card>)}{state.tasks.every(task => !task.dueAt) ? <Card span="12"><div className="bl-empty">Add due dates to tasks to populate Calendar.</div></Card> : null}</div>;
 
-  const renderSettings = () => <div className="bl-grid"><Card title="Runtime settings" span="6"><div className="bl-form"><div className="bl-field" data-span="12"><label>Actor name</label><input value={state.settings.actorName} onChange={event => commit('SETTING_CHANGED', 'actorName', draft => { draft.settings.actorName = event.target.value; })} /></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.workerReadGate} onChange={event => commit('SETTING_CHANGED', 'workerReadGate', draft => { draft.settings.workerReadGate = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Worker read gate</label></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.requireEvidenceForDone} onChange={event => commit('SETTING_CHANGED', 'requireEvidenceForDone', draft => { draft.settings.requireEvidenceForDone = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Evidence required for DONE</label></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.localOnly} onChange={event => commit('SETTING_CHANGED', 'localOnly', draft => { draft.settings.localOnly = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Local-only runtime store</label></div></div></Card><Card title="Backup and restore" span="6"><p>Export includes projects, rules, receipts, tasks, workers, evidence, audit, approvals, bugs, archetypes and connector metadata. Secret values are never part of this state.</p><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" data-primary="true" onClick={exportState}>Export JSON</button><label className="bl-btn">Import JSON<input type="file" accept="application/json" hidden onChange={importState} /></label><button className="bl-btn" data-danger="true" onClick={() => { const fresh = createDefaultBrainlinkState(); saveBrainlinkState(fresh); setState(fresh); setToast('Local Brainlink state reset.'); }}>Reset local state</button></div></Card><Card title="Storage" span="12"><div className="bl-code">{BRAINLINK_STORAGE_KEY}\nschemaVersion: {state.schemaVersion}\nmode: {state.settings.localOnly ? 'LOCAL_ONLY' : 'ADAPTER_READY'}\naudit events: {state.audit.length}\nevidence records: {state.evidence.length}</div></Card></div>;
+  const renderSettings = () => <div className="bl-grid"><Card title="Runtime settings" span="6"><div className="bl-form"><div className="bl-field" data-span="12"><label>Actor name</label><input value={state.settings.actorName} onChange={event => commit('SETTING_CHANGED', 'actorName', draft => { draft.settings.actorName = event.target.value; })} /></div><div className="bl-field" data-span="12"><label>Organization ID</label><input value={state.settings.organizationId} onChange={event => commit('SETTING_CHANGED', 'organizationId', draft => { draft.settings.organizationId = event.target.value || 'local'; })} /></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.workerReadGate} onChange={event => commit('SETTING_CHANGED', 'workerReadGate', draft => { draft.settings.workerReadGate = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Worker read gate</label></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.requireEvidenceForDone} onChange={event => commit('SETTING_CHANGED', 'requireEvidenceForDone', draft => { draft.settings.requireEvidenceForDone = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Evidence required for DONE</label></div><div className="bl-field" data-span="12"><label><input type="checkbox" checked={state.settings.localOnly} onChange={event => commit('SETTING_CHANGED', 'localOnly', draft => { draft.settings.localOnly = event.target.checked; })} style={{ width:'auto',marginRight:8 }} /> Local-only runtime store</label></div></div></Card><Card title="Backup and restore" span="6"><p>Export includes projects, rules, receipts, tasks, workers, evidence, audit, approvals, bugs, archetypes and connector metadata. Secret values are never part of this state.</p><div className="bl-actions" style={{ marginTop:12 }}><button className="bl-btn" data-primary="true" onClick={exportState}>Export JSON</button><label className="bl-btn">Import JSON<input type="file" accept="application/json" hidden onChange={importState} /></label><button className="bl-btn" data-danger="true" onClick={() => { if (!window.c